# deepVogue — training dependencies (Colab GPU runtime).
# Install on top of requirements.txt:  pip install -r requirements-train.txt

torch>=1.9         # no upper bound: keep Colab's preinstalled 2.x (conv2d_gradfix falls back cleanly)
torchvision
# pyspng intentionally omitted: optional PNG fast-path, fails to build on
# py3.12; training/dataset.py falls back to PIL when absent.
pandas
scikit-learn
librosa            # audio-reactive walks
ninja>=1.10        # StyleGAN3 CUDA op JIT build
scipy>=1.7         # SG3 signal/optimize used in networks_stylegan3.py
pillow>=9
