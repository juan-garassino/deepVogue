# deepVogue — runtime dependencies (lightweight, importable on local CPU box).
# For Colab/GPU training also install requirements-train.txt.

# packaging
pip>=21
setuptools>=42
wheel>=0.36

# CLI / utilities
click>=8.0
tqdm
psutil
requests
six>=1.14
joblib
memoized-property
termcolor
colorama

# image / array
numpy
scipy
Pillow
imageio
imageio-ffmpeg==0.4.3
opensimplex

# optional during inference (kept light); training pulls full torch stack via requirements-train.txt
matplotlib

# tests / linter
pytest
coverage
flake8
black
